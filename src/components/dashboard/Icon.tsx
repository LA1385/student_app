// Dashboard-specific icon map — extend when adding dashboard navigation icons
// import { Home, Hammer, AlertCircle, LucideIcon } from 'lucide-react';

/* const iconMap: Record<string, LucideIcon> = {
    'Home': Home,
    'Progress': ...,
    'Tools': Hammer,
    'Task': ...,
    'Zip': ...,
    'Converter': ...,
    'Calculator': ...,
    'File Compression': ...,
};

type IconProps = {
    name: string;
    className?: string;
};

export default function Icon({ name, className }: IconProps) {
    const LucideComponent = iconMap[name] || AlertCircle;
    return <LucideComponent className={className} />;
} */
